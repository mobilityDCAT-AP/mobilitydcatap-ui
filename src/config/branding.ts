export const brand = {
  primary: '#002060',
  secondary: '#FFDC06',
  logoUrl: `${import.meta.env.BASE_URL}logo.png`,
  appName: 'mobilitydcat-ap Metadata Generator Tool',
  companyName: 'Cefriel',
  companyUrl: 'https://www.cefriel.com/',
  /** UI language for messages and form locale (e.g. 'en', 'it'). Defaults to 'en'. */
  locale: undefined as string | undefined,
  /** Preferred language for SKOS vocabulary labels. Defaults to each vocabulary's own language when undefined. */
  vocabularyLabelLang: undefined as string | undefined,
}
